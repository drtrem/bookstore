<h2>Forgot your password?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "Send me reset password instructions" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>

.text-center
  %h1.mb-30 Forgot your password?
  %p.general-password-text We'll email you instructions on how to reset it
= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'general-form' }) do |f| %>
  = f.label :email, 'Enter Email', { class: 'control-label font-weight-light', for: "email" }
  = f.email_field :email, { class: 'form-control', id: 'email', name:"email", type: "email", autofocus: true, placeholder: "Enter your email" }
  = f.submit "Email Instructions", {class: 'btn btn-default center-block mb-20'}
  .text-center
    %a.in-gold-500.mb-20{href: "#"} Cancel